// Package authority owns the minimal, single-node R7 durable authority.
//
// It deliberately knows only the closed consequences defined by agency. Open
// semantic labels remain opaque Event content and never select storage code.
package authority
